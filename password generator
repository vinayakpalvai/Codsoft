import random
print("no of elements required:")
n=int(input())
all_characters=[]
for i in range(33,120):
    all_characters.append(str(chr(i)))
generated_password=""
for i in range(n):
    generated_password=generated_password+all_characters[random.randint(0,len(all_characters))]
print(generated_password)
